name: CI Build Test

on:
  push:
    branches: [main]
    paths-ignore: ['version.json']   # version.json 变更时不触发 build.yml
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  desktop:
    uses: ./.github/workflows/_build-desktop.yml

  android:
    uses: ./.github/workflows/_build-mobile.yml
